services:
  app:
    build:
      context: .
      target: dev
    working_dir: /extension
    command: "uv run swoext run --local"
    volumes:
      - .:/extension
    env_file:
      - .env
    ports:
      - "8080:8080"
